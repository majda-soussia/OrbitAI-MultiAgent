# config/llm.yaml
model:
  name: "qwen2.5:7b"
  provider: "ollama"
  base_url: "http://localhost:11434"

parameters:
  temperature: 0.3
  top_p: 0.8
  top_k: 40
  repeat_penalty: 1.1
  max_tokens: 2048

language:
  default: "en"
  supported: ["en", "fr", "ar"]